package ic_scan;

import ic_scan.scanner.NetworkInfo;

public class application {
    public static void main(String[] args) {
        NetworkInfo NI = new NetworkInfo();
        NI.getInfo();
    }
}
